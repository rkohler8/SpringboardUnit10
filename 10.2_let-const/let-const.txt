ES2015 Global Constants:
const PI = 3.14
PI = 42;

Quiz:
1)  You can redeclare and reassign values with var. var will hoist to the top of the scope it is defined in.
     let creates a block-scoped variable. let  can be reassigned but not redeclared
1)  You can redeclare and reassign values with var. var will hoist to the top of the scope it is defined in.
    const prevents a variable from ever being reassigned or redeclared. const is also block-scoped, like let.
3)  const cannot be directly changed while let can be manipulated within it's block scope.
4)  hoisting is essentially establishing when a variable can be accessed within its scope whether it has been declared yet or not.